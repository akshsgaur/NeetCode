class solution:

    def MyGroupAnagram(self, strs):
        return_list = []
        len_of_string = len(strs)
        for i in len_of_string:

            return_list.append(strs[i])
            for j in range(len_of_string):
                pass
            